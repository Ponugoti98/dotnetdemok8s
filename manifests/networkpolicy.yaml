apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-same-namespace
  namespace: sincehetoldmetoo
spec:
  podSelector:
    matchLabels:
      app: dotnet-app
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
